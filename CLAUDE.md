# コーポレートサイト（nextb.net）制作規約

## 日本語コピーの規範（全ページ・LP共通）

**サイトコピーを書く・直す・レビューするときは `site-copywriting` スキルを起動する**（2026-07-17 導入。23社調査に基づく規則 R-01〜R-27・grep lint・失敗類型集の運用正本。調査根拠は同スキルの references/evidence.md）。本節は必須規則（R-01〜R-06）と R-22・R-27 の要点の正本で、矛盾時は本節を優先する。コラム記事本文には文体規則を適用しない（次節の `column-writing` の領分）。ただし必須規則 R-01〜R-04（事実性・契約形態・料金・煽り禁止）はコラム本文を含む自社の地の文すべてに適用する。

- **事実にない情報を創作しない**（R-01）
  - 数字・実績・顧客の発言は、実在し確認できるものだけを書く
  - 編集調に書き直した要約文を「顧客の声」として載せない
- **契約・料金の表記を誤読なく書く**（R-02・R-03。決定の正本は下記「運用メモ」）
  - 準委任のみ。表記は「顧問契約」「プロジェクト型支援」でカッコ書き補足なし
  - 請負と誤読される肯定表現（「一括」「完成させます」等）を書かない。請負の明示的否定はむしろ推奨
  - 料金は目安レンジ表記（顧問契約 月10万円〜／プロジェクト型支援 30万円〜、税別）
- **煽り・過大約束をしない**（R-04）
  - 恐怖・緊急訴求（「今すぐやらないと危険」「手遅れになります」）を使わない
  - 見出し・CTAに感嘆符を使わない
  - 期待を吊り上げる修飾（「必ず」「即解決」「すべて解決」）を使わない
  - 切迫感を出してよいのは検証可能な事実（サポート終了日等）に限る
- **見出しは名詞句を基本にする**（R-05）: 数え上げ宣言文（「〜は、2つです」）を見出しにしない（市場調査でも0/23社。ユーザー指摘 2026-07-11）
- **不自然な読点を使わない**（R-06。代表例: 助詞直後で切る読点）
  - 「キッティングを、自動化する」型を新規コピー・見出しで使わない
  - 例外: 確立ブランドコピー2本（「あなたのチームに、もうひとりの情シスを。」「導入するだけのITから、使われるITへ。」）のみ字句そのまま維持
- **対比構文は配置と量をページ単位で管理する**（R-22）
  - カウント単位はユニークなコピー文（共通部品経由の描画回数・掲載ページ数では数えない）
  - サイト共通ブランドコピーは別枠。対比構文を含むのは「導入するだけのITから、使われるITへ。」のみ
  - 新規の対比は1ページにつき宣言バンドの1箇所までを目安とし、超過はレビューで理由を言えること
  - H1・OGP・特長見出し・FAQ回答・事例成果文・料金リード文は肯定形で直接述べる
  - 「請負契約は承っていません」型の事実の否定は対比に数えない
- **英語翻訳調を避け、日本のB2Bサイト慣用に合わせる**（R-27）
  - 「こんなお悩みはありませんか？」「選ばれる理由」「支援の流れ」等の定型は許容
  - 疑問形見出しの限定・英字eyebrowの扱いはスキルの R-23・R-25 に従う（正本はスキル側）
- **提示前の点検**: スキルの「自己点検」の grep lint（数え上げ宣言文・助詞直後読点〔見出し級に限定〕・対比構文・煽り語・請負誤読語・料金表記・文末反復など）を走査してから提示する。grep は警告用であり、自動修正はしない

## コラム本文のライティング規範

コラム本文（`src/content/articles/`・`_stock/articles/`）を新規に書く・推敲する・レビューするときは **`column-writing` スキルを起動する**（2026-08-02 導入）。記事の設計・語り・事実の確度・専門用語・節と文のリズム・失敗パターンの検出・執筆前後の点検手順の正本はスキル側にあり、本節は適用範囲と、上書きできないユーザー決定の正本である。矛盾時は本節を優先する。

- **実体は `.claude/skills/column-writing/`**（このプロジェクト限定。他の ~/Claude サブプロジェクトには適用されない）
- **経緯**（2026-08-02 ユーザー決定）: 従来は `japanese-tech-writing` と `cognitive-rhythm-writing` の2スキルを併用していたが、ジャンル不適合が判明したため統合し、旧2スキルは削除した（復元が要るときは git 履歴から取り出す）
  - `cognitive-rhythm-writing` の中核（常に未回収の緊張を開いておく・考えながら進む当事者の声・問いを半分ずつ返す）が、調べ物として読みに来る読者と衝突していた
  - CLAUDE.md 側の上書きが4種類まで増え、土台の選び方が合っていないことが明らかになった
  - 両スキルから流用できる部分は `column-writing` に取り込み済み。**コラムでは旧2スキルを使わない**
- **コラム本文以外の文章生成では使わない**（ユーザー決定 2026-07-17）
  - LP文言・見出し・CTAなどのサイトコピー: `site-copywriting` と本ファイル冒頭の節を適用する
  - CLAUDE.md・README等のプロジェクト内ドキュメント
  - コミットメッセージ・PR説明
  - チャット応答などの会話出力
- **必須規則 R-01〜R-04（事実性・契約形態・料金・煽り禁止）はコラム本文にも適用する**。冒頭の節が正本
- **文体は「ですます調」で書く**（企業サイトのコラムのため。ユーザー決定 2026-07-17）
- **語りは「顧客に向けて説明する」姿勢にする**（ユーザー決定 2026-07-17）
  - 一般論としての解説文ではなく、相談・支援先の顧客に語りかける口調にする
  - 「貴社」への言及（書き言葉のため「御社」ではなく「貴社」に統一）、「ご確認ください」「おすすめします」等の助言口調を、前提条件・手順・注意点など助言が自然な箇所で使う
  - 本文中の助言箇所でも二人称を使ってよい
- **既存記事の一斉書き直しはしない**。適用は新規執筆・推敲の対象範囲のみで、`src/content/articles/` の既存記事に自動で遡及しない
- **手直し差分から抽出した文体規範は `column-writing` 側に集約した**（第1次 2026-07-24、第2次 2026-08-02。根拠はいずれもユーザーの手動修正）
  - 第1次: 頻度・実績の表現／読者を追い込む断定／冒頭のつかみ引用／箇条書きの文末／太字の単位
  - 第2次: 助言は推奨を先に言い切る／抽象的な機構より場面で書く／拍のためだけの短文を足さない／語釈も不要なら置かない

## デザイン・素材の規約

- **LPは `lp-*` コンポーネントを共用する**（src/styles/global.css の「LP 共通部品」セクション）
  - 標準構成: ヒーロー → お悩み → 宣言バンド → 3つの特長 → 中間CTA → 支援の流れ → 事例 → FAQ → CTA
- **コンポーネント化の線引き**（2026-07-16 導入。過剰共通化を防ぐための基準）
  - **データ駆動にするもの**（構造が固定的な下回り）: 支援の流れ `LpSteps`・FAQ `LpFaq`・誘導バナー `LpBanner`・中間CTA `LpConsult`・事例 `CaseCard`（正本は src/data/cases.ts）
  - **直書きを維持するもの**（コピーと意匠を作り込む上物）: ヒーロー・宣言バンド・お悩み・特長・フロー図・Before/After
    - 理由: LPごとに構成が既に分岐しており、props に畳むと編集コストが逆に上がる。構造と変更理由が収束したら再評価する
- **CSSの命名**: 新規モディファイアは BEM 風の `--` 形式（`.card--link` 等）。既存の単一ハイフン（`btn-primary`・`section-gray` 等）は互換のため維持する
- **CSSの色・影はセマンティックトークンを使う**（:root の `--accent-soft`・`--muted-on-dark`・`--shadow-card` 等）。同じ意味の色を hex 直書きで増やさない
- **イラストはソコストから調達する**（https://soco-st.com、商用利用可・クレジット不要）
  - SVG直リンク: `https://soco-st.com/wp-content/themes/socost/upload/<ID>_color.svg`
  - オレンジ系（#febe69・#ffda71 等）はブランド青 #8fb0f0 に置換してから `public/images/ill-*.svg` に配置する
- **図解（フロー図・対比図とも）はHTML/CSS部品で作る**（`.lp-flowfig` `.lp-ba` 等）
  - **このサイトでは Napkin 生成画像を使わない**（ユーザー判断 2026-07-14）
    - 経緯: PC表示のみ Napkin 画像に出し分ける方式を3つのLPで導入したが（2026-07-12）、図が適切でなく全点不採用・HTML/CSS 図に差し戻した
    - Napkin は対比図も苦手（内容の捏造・平板化を実測 2026-07-12）
    - ~/Claude/CLAUDE.md の図表生成節（Napkin 既定）より本規約を優先する
  - インラインSVGも使わない: 縮小時に文字が潰れる（ユーザー指摘 2026-07-10）

## 運用メモ

- **デプロイ**: main へ push すると GitHub Actions（.github/workflows/deploy.yml）が GitHub Pages に自動デプロイする
  - CI の verify ジョブが型検査（astro check）・OG画像の存在・canonical の実URL一致・内部リンクを検証し、失敗するとデプロイされない
- **dist/ はビルド成果物**（リポジトリ管理外）。CI が build するのでローカル build は検証用
- **sitemap.xml はビルド時に自動生成**（scripts/sitemap-integration.mjs が dist の実ファイルから生成。手動追記は不要。noindex ページと404は自動除外）
- **canonical は原則自動導出**。ルート直下の `.html` ページ（about・services 等）のみ BaseLayout に明示指定する（実URLとの一致はビルドが検証する）
- **コラム公開手順**: _stock/ から md と OG 画像 png を mv して push するだけ（お知らせ・一覧・sitemap は自動生成。OG 画像の置き忘れは CI が検知）
- **料金の目安**（ユーザー決定 2026-07-11）: 顧問契約 月10万円〜（税別）／プロジェクト型支援 30万円〜（税別）。金額は目安レンジ表記とする
- **解約条件**（ユーザー決定 2026-07-17）: 顧問契約に最低契約期間は設けない。解約は1ヶ月前までの申し出
- **既存ベンダー・社内担当との役割分担**（ユーザー決定 2026-07-17）: 既存の契約・役割はそのままで、社内側の相談相手として並走する形。ベンダーへの発注・契約・調整の代行は支援範囲外
- **契約はすべて準委任のみ**（ユーザー決定 2026-07-11）
  - 請負と誤読される肯定表現（「一括」「成果物を決めて進める」「完成させます」等）をサイトに書かない
  - 「成果物の完成をお約束する請負契約は承っていません」のように、請負を明示的に**否定する**文はむしろ推奨（料金ページの注記・FAQで使用中）
  - 契約形態の表記は「顧問契約」「プロジェクト型支援」とし、カッコ書きの補足（（月額）（準委任）等）は付けない（ユーザー決定 2026-07-11）
  - 準委任である旨は、見出しではなく注記・FAQの本文で明示する
